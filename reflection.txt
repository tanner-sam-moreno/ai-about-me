1. I used ChatGPT to generate me the HTML and CSS code for this assignment.
2. "Create an 'About Me' section for a portfolio website using HTML and CSS only (no JavaScript). It should include: - A profile photo on the left (use a placeholder image for now) - My name as a heading, just list it as (NAME HERE) and I can type it in - A short bio paragraph (just list the space as (PARAGRAPH HERE) and I can type it in. - A list of 4 skills displayed as tags/badges - Use "lorem ipsum" instead of my information and I can enter that in later. Make it responsive - should stack vertically on mobile. Use a modern, clean design with a light color scheme. Add comments explaining the CSS." 
3. I used 2 prompts to generate me the initial HTML and CSS, (one to start with, and the second to separate the HTML and CSS on different codes, because it originally generated it all on the HTML at first with CSS elements in it. I then used a third and final prompt to generate the final tweaks.
4. With the CSS, can you: - Make the font a more vintage looking font, but one that's still readable - A color palette that pairs well with the chosen font - Have the panels take up more of the screen with less whitespace, allowing the page to breathe and not look so cluttered.
5. I don’t know if this exactly answers the question but looking at AI code could actually be a good “example” study tool. Looking at the code and seeing how it built certain things (like the flexbox), it’s almost kind of how I would study math equations in school. Look at how it was done in the example and take what you learned from it to understand how to build your own version.